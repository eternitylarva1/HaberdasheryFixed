{
  "modid": "${mod.id}",
  "name": "${mod.name}",
  "author_list": ["${mod.authors}"],
  "credits": "${mod.credits}",
  "description": "${mod.description}",
  "version": "${mod.version}",
  "sts_version": "${mod.sts_version}",
  "mts_version": "${mod.mts_version}",
  "dependencies": ["${mod.dependencies}"]
}
